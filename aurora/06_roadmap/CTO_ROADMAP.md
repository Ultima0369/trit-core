# Aurora CTO 路线图：注意力主权训练系统

**版本**: 0.2.0  
**日期**: 2026-06-30  
**状态**: 草案 — 待 CTO/创始人确认后冻结  
**分类**: 06_roadmap — 技术与产品战略规划  
**叙事基准**: 对齐 `docs/NARRATIVE_CHARTER.md`

---

## 一、产品定位（一句话）

> **Aurora 是一个注意力主权训练系统 + 长见识输入源。它以"提醒注意力指向"为起点，通过 Frame 标注、三值决策、节律分析、注意力调度，以及持续的长见识输入（地理/生态/文化），训练用户不被现象劫持，撑开被默化收窄的参考系，最终长成自己的 trit-core。**

### 1.1 与传统效率工具的区别

| 维度 | 传统效率工具 / AI 助手 | Aurora |
|------|----------------------|--------|
| 目标 | 帮用户更快做更多事 | 帮用户看见自己的注意力被什么捕获，并用长见识撑开参考系 |
| 对冲突的态度 | 消抹冲突，给出"最佳"答案 | 保留冲突，输出 Hold |
| 对世界的态度 | 只看"此刻的人+此刻的钱"，无视地理/生态/文化底层塑造 | 把地理/生态/文化作为根本输入源灌入参考系 |
| 与用户的关系 | 越懂你越好 | 越不操控你越好 |
| 成功指标 | DAU / 留存 / 任务完成数 / 收入 | 注意力自主性提升、参考系被撑开、用户最终毕业（反指标） |
| 分发 | 获客、拉新、转化 | 开源免费、不争注意力、自我筛选 |
| 最终交付 | 用户离不开产品 | 用户不再需要产品 |

### 1.2 核心用户价值

1. **觉察**：用户能实时看到自己的注意力指向哪个 Frame（身体、角色、社交、环境等）。
2. **悬置**：当多个 Frame 冲突时，系统不替用户决定，而是提醒"这里需要悬置"——Hold 是"此刻不该塌缩，因为参考系还撑不起"。
3. **长见识**：通过地理/生态/文化的持续输入，撑开被默化收窄的参考系，扩展用户下一次决策的可选项空间。
4. **训练**：通过 AttentionScheduler 的 ShiftTo / HoldCurrent / Recalibrate 建议，训练用户主动调度注意力。
5. **检验**：通过节律分析（基频稳定性、相位恢复速度）验证训练是否有效。

---

## 二、北极星指标与反指标

### 2.1 北极星指标

> **注意力自主性指数（Attention Sovereignty Index, ASI）** + **参考系撑开度（Frame Expansion）**

ASI 定义（M0 简化版）：

```
ASI = (用户主动调度次数) / (系统提醒次数) × (相位恢复速度系数)
```

- **用户主动调度次数**：用户在收到提醒后，执行了 ShiftTo / HoldCurrent 选择的次数。
- **系统提醒次数**：系统检测到注意力被现象劫持并发出提醒的次数。
- **相位恢复速度系数**：环境冲击后，用户基频回到基线的速度（相对于历史基线）。

ASI 越高，说明用户越能自主管理注意力，系统越成功。

**参考系撑开度（Frame Expansion）**：用户使用前后，决策可选项空间的扩展程度。长见识输入的根本目的不是让用户"这次决策更好"，而是**扩展用户下一次决策的可选项空间**。这是"长见识"是否起作用的直接度量。

### 2.2 阶段性指标

| 阶段 | 指标 | 目标 |
|------|------|------|
| M0 | 提醒准确率 | 跨 Frame 冲突检测准确率 > 95% |
| M0 | 基频检测误差 | 合成数据 < 1% |
| M1 | 主动调度率 | 用户收到提醒后主动调度 > 30% |
| M1 | 相位恢复速度 | 相比未使用系统时提升 > 10% |
| M2 | 角色入侵识别率 | 模拟角色入侵场景 > 90% |
| M2 | 环境冲击预警提前量 | > 1 小时 |
| M3 | 团队级相变检测延迟 | < 5 分钟 |
| M4 | 企业部署数据主权合规率 | 100% |

### 2.3 反指标（必须抑制）

以下指标上升，说明产品背叛了定位：

- **用户对系统建议的依赖率**（应该下降）
- **系统打开时长 / 日均使用次数**（不应追求）
- **用户画像维度数**（禁止增长）
- **云端数据同步频率**（应尽可能低）
- **推送通知频率**（应仅在关键时刻触发）

---

## 三、技术架构：注意力训练闭环

```
┌─────────────────────────────────────────────────────────────────┐
│                        现象层（Phenomena）                        │
│  邮件元数据 · 日历事件 · 通知时间 · 位置变化 · 生理信号 · 环境数据    │
│  原则：只采元数据，不读内容；本地存储；用户可随时关闭任一数据源       │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        感知层（Perception）                       │
│  通信频率时间序列 → 小波分析（CWT/DWT）                            │
│  输出：基频 f₀ · 谐波 · 相位漂移 Δφ · 频谱重构 DKL                  │
│  输出：环境冲击等级 · 角色边界渗透指数                              │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        标注层（Annotation）                       │
│  Embodied（身体）· Individual（个人）· Relational（关系）            │
│  Role（角色）· GeoEco（地理生态）· Environmental（环境）             │
│  Phase：注意力倾向程度 [0.0, 1.0]                                  │
│  Confidence：信噪比 / 小波功率 / 数据完整性                         │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        运算层（Computation）                      │
│  Trit-Core：TAND / TOR / TNOT / 跨域冲突检测                        │
│  ResolutionPolicy：按 Domain 仲裁（Organizational / Relational 等） │
│  SecurityMode：Service / Refusal / Awareness / Transparency        │
│  AttentionScheduler：ShiftTo / HoldCurrent / Recalibrate           │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        反馈层（Feedback）                         │
│  提醒："你的注意力当前被 [Role] 捕获"                              │
│  训练：提供 ShiftTo / HoldCurrent 选项，用户选择                    │
│  记录：用户覆盖 / 采纳 / 忽略，全部写入审计日志                      │
│  检验：对比训练前后的节律稳定性与相位恢复速度                        │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        呈现层（Presentation）                     │
│  注意力图谱（雷达图）· 冲突面板 · 节奏报告 · 决策审计                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.1 关键设计约束

1. **本地优先**：所有数据默认本地处理，不上云。
2. **元数据优先**：不读取邮件内容、不读取聊天记录、不读取文档内容。
3. **不进化**：系统出厂设置不变，不基于用户数据学习。
4. **可覆盖**：任何系统输出，用户都可以忽略或覆盖。
5. **可断开**：用户可随时导出、删除、卸载，系统不挽留。

---

## 四、开发阶段与里程碑

### M0 概念验证（2026-06-20 → 2026-09-20）

**目标**：证明"小波 + 三值 + 注意力提醒"概念可行。

**已完成**：
- [x] Workspace 骨架与 Aurora crate
- [x] 合成数据 + FFT 基频检测
- [x] Embodied vs Individual 跨域冲突检测
- [x] CLI + JSON/HTML 输出
- [x] SecurityMode 状态机 + 10 个伦理门禁测试
- [x] trit-core 安全硬化（Frame/Domain/PolicyViolation 扩展）

**M0 剩余工作**（按优先级排序）：

| 优先级 | 任务 | 验收标准 | 负责人 |
|--------|------|----------|--------|
| P0 ✅ | 邮件采集抽象层 + JSON fallback | `cargo test ingest` 通过；DataSource trait + IngestManager 完成 | 已完成 |
| P0 ✅ | 注意力调度最小闭环 | AttentionSession + AttentionManager → run_cycle → respond；ASI 计算 | 已完成 |
| P0 ✅ | 注意力图谱 HTML 渲染 | HTML 含 ASI 仪表条 + 冲突面板 + 提醒历史表，浏览器可读 | 已完成 |
| P1 | 作者自我验证 | 作者用自己的数据跑通端到端流程 | 创始人 |
| P1 | 2 个外部用户试用反馈 | 收集问卷或访谈记录 | 创始人 |

**M0 Exit Criteria**：
- [x] `cargo test --workspace --all-features` 通过
- [x] `cargo test ethics_` 10 项全部通过
- [x] `cargo clippy --workspace --all-targets --all-features -- -D warnings` 通过
- [ ] 日数据分析 < 1 秒
- [ ] TECH_REVIEW_CHECKLIST.md 全部勾选

---

### M1 单用户 MVP（2026-09-21 → 2026-12-20）

**目标**：发布可安装的桌面应用，建立首批自愿试用用户群（开源免费，不收费）。

**功能范围**：

| 功能 | 说明 | 优先级 |
|------|------|--------|
| Tauri 桌面应用 | macOS / Windows / Linux 安装包 | P0 |
| 多数据源 | 邮件 + 日历 + 手动输入 | P0 |
| 实时注意力图谱 | 可交互的雷达图，显示当前 Frame 权重 | P0 |
| 冲突面板 | 跨域冲突列表，Hold 可覆盖 | P0 |
| 长见识输入（地理/生态） | 接入公开气候/地理数据，灌入 GeoEco/Science Frame | P0 |
| 节奏报告 | 周/月节律报告，PDF 导出 | P1 |
| 设置面板 | 数据源开关、阈值、SafeFallback 开关 | P0 |
| 数据导出 | JSON / SQLite 导出，无锁定期 | P0 |

**M1 Exit Criteria**：
- 桌面应用可打包安装
- 离线可用
- 启动 < 3 秒
- 单用户内存 < 500MB
- 至少 10 名自愿试用者主动找来并给出反馈（不收费、不推广）
- 反指标达标：试用者对系统建议的依赖率呈下降趋势、打开时长不被追求
- 试用者中至少 1 人报告"长见识输入改变了某个决策的可选项"

---

### M2 深度试用与长见识扩展（2026-12-21 → 2027-04-20）

**目标**：服务公众人物、演员、高性能个体，深化长见识输入与文化/风俗内容填充（开源免费，不收费）。

**功能范围**：

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 角色边界监控 | 角色入侵检测、解离预警（Awareness 通知） | P0 |
| 环境相位冲击 | 环境切换检测、分级预警、恢复曲线 | P0 |
| 文化/风俗内容填充 | 后天文化塑造作为 Consensus Frame 输入 | P0 |
| 决策审计 | 每个输出附带完整 MetaInterrupt 日志 | P1 |
| 高级可视化 | 小波尺度图、相位漂移图 | P1 |
| 第三方安全审计 | 渗透测试 + 加密方案审计 | P1 |

**M2 Exit Criteria**：
- 至少 5 名深度试用者持续使用并给出案例反馈（不收费）
- 完成 2 个可公开的用户案例（脱敏，用户自愿）
- 第三方安全审计无高危漏洞
- 反指标达标：用户画像维度数仍为 0（禁止增长）、云端同步频率仍为 0

---

### M3 组织级自托管（2027-04-21 → 2027-08-20）

**目标**：支持组织级自部署，监控关键节点相变（开源免费，可自托管，不收费）。

**功能范围**：

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 本地服务器 | 团队数据可选同步到本地服务器（端到端加密） | P1 |
| 节点拓扑 | 组织内人际关系与业务关系网络 | P0 |
| 节点相变检测 | 关键人物相位变化检测 | P0 |
| 级联风险 | 相变的级联影响计算 | P0 |
| 团队注意力图谱 | 多人注意力聚合与冲突 | P1 |

**M3 Exit Criteria**：
- 至少 2 个团队自部署并反馈（不收费）
- 支持 100 人团队
- 个人数据本地加密，企业无法解密
- 反指标达标：无任何基于用户数据的在线学习代码

---

### M4 生态成熟（2027-08-21 → 2027-12-20）

**目标**：生态成熟，长见识输入源覆盖全球气候/生态/地理/风俗，用户毕业路径清晰（开源免费）。

**功能范围**：

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 自托管部署 | 组织内网完整运行 | P0 |
| 自定义 Frame/Domain | 高级用户可扩展自己的参考系 | P1 |
| 长见识内容生态 | 全球气候/生态/地理/风俗内容持续填充 | P0 |
| 集中审计 | 组织级审计日志（员工仍可导出个人副本） | P1 |

**M4 Exit Criteria**：
- 至少 1 个长期用户报告"已基本不再需要系统辅助"（毕业信号）
- 长见识输入源覆盖主要气候带与文化圈
- 反指标全面达标：依赖率下降、画像维度为 0、云同步为 0、推送频率仅关键时刻

---

## 五、工程学纪律

### 5.1 代码纪律

1. **伦理门禁测试不可跳过**
   ```bash
   cargo test ethics_
   ```
   失败 = 禁止发布。

2. **无 unsafe 代码**
   Aurora crate 使用 `#![forbid(unsafe_code)]`。

3. **CI 流水线**
   ```
   cargo fmt --check
   cargo clippy --workspace --all-targets --all-features -- -D warnings
   cargo test --workspace --all-features
   cargo test ethics_
   cargo audit
   cargo build --release
   ```

4. **ADR 驱动变更**
   - 修改 CHARTER / FIRST_PRINCIPLES 核心内容：禁止。
   - 修改 Frame / Domain enum：需要 ADR。
   - 新增数据源：需要 ADR。
   - 新增网络依赖：原则上禁止，除非 ADR 特批。

5. **文档同步**
   - 修改代码 → 同步 `map/06_code.md`
   - 新增概念 → 同步 `map/` MOC 和 `docs/`
   - 新增 ADR → 同步 `map/03_adr.md`

### 5.2 设计纪律

1. **任何功能必须有关闭开关**
   - 用户可关闭任何数据源、任何提醒、任何安全检查。
   - 关闭行为本身被记录，但不阻止。

2. **Awareness 不阻断**
   - `SecurityMode::Awareness` 下，代码中不允许 `return Err` 或 `panic!`。
   - 只允许返回 `MetaInterrupt` 通知。

3. **Hold 时不猜测**
   - 当系统输出 `Hold` 时，不允许提供"最可能"答案或概率排序。

4. **不基于用户数据学习**
   - 禁止 RLHF、禁止微调、禁止用户画像、禁止 A/B 测试。

### 5.3 发布纪律

每次发布前必须：

1. 完成 `TECH_REVIEW_CHECKLIST.md` 全部检查项。
2. 由至少 1 名开发者逐项勾选并签名。
3. 任何未勾选项必须有 ADR 记录。
4. 发布版本号遵循 SemVer。
5. 发布说明中包含伦理门禁测试结果。

---

## 六、验收标准分层

### 6.1 自动化验收（每次提交）

| 检查项 | 命令 | 通过标准 |
|--------|------|----------|
| 格式 | `cargo fmt --check` | 无差异 |
| Lint | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 无警告 |
| 单元/集成测试 | `cargo test --workspace --all-features` | 全部通过 |
| 伦理门禁 | `cargo test ethics_` | 全部通过 |
| 依赖审计 | `cargo audit` | 无高危漏洞 |

### 6.2 功能验收（每个 Milestone）

参见各阶段 Exit Criteria。

### 6.3 定心盘验收（每次发布）

使用 `aurora/04_engineering/TECH_REVIEW_CHECKLIST.md`：

- 不剥夺（8 项）
- 不自欺（6 项）
- 不进化（5 项）
- 公开可审查（6 项）
- 安全模型（8 项）
- 数据主权（8 项）
- 性能与可靠性（7 项）
- 依赖安全（6 项）
- 文档一致性（6 项）

共 60 项。发布前必须全部勾选或写 ADR。

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 小波分析性能/精度不足 | 中 | 高 | M0 用 FFT 证明概念；M1 引入 CWT；持续用合成数据验证 |
| 用户不理解 Hold 设计 | 高 | 中 | 文档强调"Hold 不是失败，是长见识腾出的空间"；UI 明确解释冲突结构 |
| 本地优先限制功能扩展 | 中 | 中 | 用 Tauri + 本地 SQLite；组织级自托管才引入可选本地服务器 |
| 长见识输入源覆盖不全 | 中 | 中 | 渐进填充：气候/生态先于风俗/文化；公开数据优先，不依赖专有源 |
| 项目无收入导致维护停滞 | 中 | 高 | 创始人自持 + 自愿贡献者；严格限范围不做 SaaS；接受"项目很慢"为正常 |
| 数据源格式频繁变化 | 高 | 中 | 抽象 DataSource trait；JSON fallback 作为保底 |
| 安全审计发现漏洞 | 低 | 极高 | M2 引入第三方审计；M0-M1 用 cargo-audit + 属性测试 |

---

## 八、Immediate Next Steps（未来 2 周）

1. **确认本路线图**：创始人/CTO 审阅并批准。
2. **M0 已完成的 P0 任务**（见 §四，勿重复）：邮件采集抽象层 + JSON fallback、注意力调度最小闭环、注意力图谱 HTML 渲染——均已 ✅。
3. **推进 M0 剩余 P1 任务**（创始人侧）：
   - 作者自我验证：创始人用自己的数据跑通端到端
   - 等待自发反馈：项目放 GitHub 开源，不推广；准备反馈收集渠道
4. **建立性能基准**：
   - 日数据分析时间基准
   - TAND/TOR 热路径延迟基准

---

*本路线图为 Aurora 的技术与产品战略规划。v0.2.0（2026-06-30）：动机表述对齐 `docs/NARRATIVE_CHARTER.md`；M1-M4 Exit Criteria 移除全部商业化指标（付费用户数/年收入/留存率/NPS），替换为反指标；"长见识输入源"纳入北极星。所有技术决策必须以 CHARTER.md 和 FIRST_PRINCIPLES.md 为最高判据。代码是最终检验。*
